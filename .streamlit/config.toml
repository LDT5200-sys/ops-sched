[server]
# Streamlit Cloud 部署配置
maxUploadSize = 10

[browser]
gatherUsageStats = false

[theme]
# 配色与 styles.py 保持一致
primaryColor = "#3e6291"
backgroundColor = "#f4f5f7"
secondaryBackgroundColor = "#f9fafb"
textColor = "#232a31"
font = "sans serif"
